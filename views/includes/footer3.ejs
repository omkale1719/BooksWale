<style>
    /* Footer Style */
    .footer-bottom {
        background: linear-gradient(45deg, #000, #444); /* Gradient background */
        color: white;
        text-align: center;
        padding: 15px 0;
        font-family: "Arial", sans-serif;
        position: relative;
        overflow: hidden;
    }

    /* Heart Animation */
    .footer-bottom p {
        display: inline-block;
        margin: 0;
        font-size: 16px;
        font-weight: 400;
        letter-spacing: 1px;
        animation: fadeInText 3s ease-in-out;
    }

    /* Heart Icon Animation */
    .footer-bottom p span.heart {
        display: inline-block;
        color: red;
        animation: beat 1.5s infinite;
    }

    /* Keyframes for Text Fade-In */
    @keyframes fadeInText {
        0% {
            opacity: 0;
            transform: translateY(10px);
        }
        100% {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* Keyframes for Heart Beat */
    @keyframes beat {
        0%, 100% {
            transform: scale(1);
        }
        50% {
            transform: scale(1.2);
        }
    }

    /* Glowing Border Animation */
    .footer-bottom:after {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        border: 2px solid rgba(255, 255, 255, 0.5);
        border-radius: 5px;
        animation: glowingBorder 4s infinite;
        pointer-events: none;
    }

    @keyframes glowingBorder {
        0% {
            box-shadow: 0 0 5px rgba(255, 255, 255, 0.5);
        }
        50% {
            box-shadow: 0 0 20px rgba(255, 255, 255, 0.8);
        }
        100% {
            box-shadow: 0 0 5px rgba(255, 255, 255, 0.5);
        }
    }
</style>

<div class="footer-bottom">
    <p>Designed with <span class="heart">❤️</span> by [Om Kale]</p>
</div>
